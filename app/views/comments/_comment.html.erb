<tr>
  <!-- Comment count, the hidden method _counter.  -->
  <!--
    See http://www.web-development-blog.co.uk/2009/02/18/57/
    When rendering a collection of partials in Rails you can access a sneaky iteration counter
    from inside each partial using the partial name appended with _counter, which starts from 0.
  -->
  <td><%= comment_counter + 1 %></td>
  <td><%= comment.commenter %></td>
  <td><%= comment.body %></td>
  <!-- Either post_comment_path(@post, comment) or [comment.post, comment] works. -->
  <td><%= link_to 'Edit' ,  [comment.post, comment] %></td>  
  <td><%= link_to 'Destroy',  [comment.post, comment] ,
        method: :delete,
        data: { confirm: 'Are you sure?' } %>
  </td>
</tr>
